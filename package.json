{
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace apps/terraform-toolkit run dev",
    "dev:backend": "npm --workspace apps/backend run dev",
    "build:backend": "npm --workspace apps/backend run build"
  }
}